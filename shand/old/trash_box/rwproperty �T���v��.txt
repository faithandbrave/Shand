#include <iostream>
#include <string>
#include <shand/property.hpp>

using namespace std;
using namespace shand;

class person {
    int    age_;
    string name_;
public:
    person()
        : age_(0), name_("")
    {
        age(this);
        name(this);
    }

    // getter(Å¶ not const)
    int    get_age()  { return age_; }
    string get_name() { return name_; }

    // setter(Å¶ argument is 'const&')
    void set_age(const int& value)     { age_  = value; }
    void set_name(const string& value) { name_ = value; }

    // property
    rwproperty<int,    person, &person::get_age,  &person::set_age>  age;
    rwproperty<string, person, &person::get_name, &person::set_name> name;
};

int main()
{
    person akira;

    // set
    akira.age  = 22;
    akira.name = "Akira";

    // get
    cout << akira.age    << endl;
    cout << akira.name() << endl;

    return 0;
}