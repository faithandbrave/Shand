#include <iostream>
#include <string>
#include <shand/property.hpp>

using namespace std;
using namespace shand;

class person {
    int    age_;
    string name_;

public:
    person()
        : age_(0), name_("")
    {
        age(this);
        name(this);
    }

    void set_profile()
    {
        age_  = 22;
        name_ = "Akira";
    }

    // getter(Å¶not const)
    int    get_age()  { return age_;  }
    string get_name() { return name_; }

    // property
    rproperty<int,    person, &person::get_age>  age;
    rproperty<string, person, &person::get_name> name;

};

int main()
{
    person akira;

    akira.set_profile();

    cout << akira.age    << endl;
    cout << akira.name() << endl;

    return 0;
}

